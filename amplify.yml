version: 1
frontend:
  phases:
    build:
      commands:
        - echo "ABS is a static site; no build step required."
  artifacts:
    baseDirectory: .
    files:
      - '**/*'
  cache:
    paths: []
